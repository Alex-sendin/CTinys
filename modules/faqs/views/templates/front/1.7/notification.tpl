<div id="mpm_faqs_form_container">
	
	<span id="mpm_faqs_form_close_button" onclick=""></span>
	
	<span class="iconcheck"></span>
	<span class="title_notify">{l s='¡Tu consulta a sido enviada!' mod='faqs'}</span>
	<span class="desc_notify">{l s='Hemos recibido tu consulta y nuestro equipo de soporte te enviará una respuesta en las próximas 24hs a tu correo electrónico: martatorres@gmail.com.' mod='faqs'}</span>
	
	  {*<header>
        <span id="mpm_faqs_form_close_button" onclick=""><i class="mpm-faqs-cancel_2"></i></span>
        <div id="mpm_faqs_form_header_content">
            {if ($status === 'success')}
                <span><i class="mpm-faqs-question-success-bubble"></i></span>
                <h1 class="title">{l s='AWESOME!'  mod='faqs'}</h1>
            {else}
                <span><i class="mpm-faqs-question-error-bubble"></i></span>
                <h1 class="title">{l s='WOOPS!'  mod='faqs'}</h1>
            {/if}

            <div id="mpm_faqs_form_header_addon"></div>
        </div>
    </header>

    <main>
        <div id="mpm_faqs_form_sub_title">{$msg|escape:'htmlall':'UTF-8'}</div>
    </main>

    <footer>
        <div class="form-group mpm-faqs-form-button-block">
            {if ($status === 'success')}
                <button class="button mpm-faqs-form-button btn btn-primary" id="mpm_faqs_notification_close_button"><span>{l s='CLOSE'  mod='faqs'}</span></button>
            {else}
                <button class="button mpm-faqs-form-button btn btn-primary" id="mpm_faqs_notification_reset_button"><span>{l s='TRY AGAIN'  mod='faqs'}</span></button>
            {/if}
        </div>
    </footer>*}
</div>